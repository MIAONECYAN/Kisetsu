[project]
name = "kisetsu-backend"
version = "0.1.0"
description = "Local FastAPI backend for Kisetsu."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "httpx>=0.27",
  "uvicorn>=0.30",
  "pydantic>=2.7",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

